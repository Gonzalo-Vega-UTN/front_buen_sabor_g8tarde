export enum Rol {
    Admin = 'Admin',
    Cliente = 'Cliente',
    // You can keep the other roles if they're used elsewhere in your application
    Empleado = 'Empleado',
    Delivery = 'Delivery',
    Cajero = 'Cajero',
    Cocinero = 'Cocinero',
  }